<Page
    x:Class="BeitieSpliter.GridsConfig"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BeitieSpliter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    RequestedTheme="Dark"
    Loaded="SettingsPage_Loaded"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


    <Page.Resources>
        <Style x:Key="LeftFriendly" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>
        <Style x:Key="LeftFriendlyBox" TargetType="TextBox">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>
        <Style x:Key="HorizontalFriendly" TargetType="FrameworkElement">
            <Setter Property="Margin" Value="0,5,0,5"/>
        </Style>
        <Style x:Key="RadioFriendly" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14.66"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Page.Resources>

    <Grid  x:Name="SettingsGrid" Grid.Row="4" Grid.ColumnSpan="4" >
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="1*"/> 
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="18*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel  Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" Padding="2" >
            <TextBlock  Text="行号：" Margin="0,0,0,0" Style="{StaticResource LeftFriendly}" />
            <ComboBox  x:Name="RowNumber" Margin="0,0,0,0"  SelectionChanged="RowNumber_SelectionChanged"  
                       VerticalAlignment="Center"
                       IsDropDownOpen="True"  Width="70"/>

            <TextBlock  Text="列号：" Margin="10,0,0,0" Style="{StaticResource LeftFriendly}" />
            <ComboBox  x:Name="ColumnNumber"  Margin="0,0,0,0" SelectionChanged="ColumnNumber_SelectionChanged"  
                        VerticalAlignment="Center"
                       IsDropDownOpen="True" Width="70" />
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <TextBlock  Text="操作对象：" Margin="10,0,0,0" Style="{StaticResource LeftFriendly}" />
                <RadioButton x:Name="OpSingleElement" Content="单字" Checked="Operation_Checked" GroupName="OperationGroup" HorizontalAlignment="Left" FontSize="14" Margin="5" IsChecked="True" />
                <RadioButton x:Name="OpSingleRow" Content="整行" Checked="Operation_Checked" GroupName="OperationGroup" HorizontalAlignment="Left" FontSize="14" Margin="5"/>
                <RadioButton x:Name="OpSingleColumn" Content="整列" Checked="Operation_Checked" GroupName="OperationGroup" HorizontalAlignment="Left" FontSize="14" Margin="5"/>
            </StackPanel>

        </StackPanel>
        <StackPanel Grid.Row="0" Grid.Column="3" Margin="10" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="BtnOk" Content="确定" FontSize="14" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Height="60" Width="100"/>

        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Vertical" VerticalAlignment="Bottom">
            <Button Margin="2" x:Name="BtnTopMinus" Content="^" Click="Adjust_Clicked" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" />
            <Button Margin="2" x:Name="BtnTopAdd" Content="∨" Click="Adjust_Clicked" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" />
        </StackPanel>

        <StackPanel  Grid.Row="3" Grid.Column="1" VerticalAlignment="Top" Orientation="Vertical">
            <Button Margin="2" x:Name="BtnBottomMinus" Content="^" Click="Adjust_Clicked"  HorizontalAlignment="Stretch" VerticalAlignment="Top" />
            <Button Margin="2" x:Name="BtnBottomAdd" Content="∨" Click="Adjust_Clicked"  HorizontalAlignment="Stretch" VerticalAlignment="Top" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="2" Grid.Column="0">
            <Button Margin="2" x:Name="BtnLeftMinus" Content="&lt;" Click="Adjust_Clicked"  HorizontalAlignment="Right" VerticalAlignment="Stretch" />
            <Button Margin="2" x:Name="BtnLeftAdd" Content=">" Click="Adjust_Clicked"  HorizontalAlignment="Right" VerticalAlignment="Stretch" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Grid.Row="2" Grid.Column="2">
            <Button Margin="2" x:Name="BtnRightMinus" Content="&lt;" Click="Adjust_Clicked"  HorizontalAlignment="Left" VerticalAlignment="Stretch" />
            <Button Margin="2" x:Name="BtnRightAdd" Content=">" Click="Adjust_Clicked"  HorizontalAlignment="Left" VerticalAlignment="Stretch" />
        </StackPanel>

        <Border x:Name="ItemBorder" Grid.Row="2" Grid.Column="1" BorderBrush="Gray" BorderThickness="1" Margin="5" >
            <ScrollViewer x:Name="ItemScrollViewer"  HorizontalScrollBarVisibility="Visible"  VerticalScrollBarVisibility="Visible"
                    ZoomMode="Enabled">

                <canvas:CanvasControl Draw="CurrentItem_OnDraw" 
                                      x:Name="CurrentItem" ClearColor="Gray" />

                <!--<Canvas Name="CurrentPage" RenderTransformOrigin="0.5,0.5"/>-->
                <!--<Image x:Name="CurrentPage" HorizontalAlignment="Center"  VerticalAlignment="Center" Source="/Assets/SplashScreen.png"/>-->
            </ScrollViewer>
        </Border>
    </Grid>
</Page>
