<Page
    x:Class="BeitieSpliter.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BeitieSpliter"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    RequestedTheme="Dark"
    Unloaded="Page_OnUnloaded" 
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <Style x:Key="LeftFriendly" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>
        <Style x:Key="HorizontalFriendly" TargetType="FrameworkElement">
            <Setter Property="Margin" Value="0,5,0,5"/>
        </Style>
        <Style x:Key="RadioFriendly" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14.66"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Page.Resources>


    <Grid x:Name="RootGrid" Grid.Row="3" Grid.ColumnSpan="2" Margin="10" BorderThickness="{ThemeResource SearchBoxThemePadding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="6*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Stretch">
            <StackPanel VerticalAlignment="Center" Orientation="Horizontal" Margin="0,0,20,0">
                <Button x:Name="ImportBtFile"  Width="200" Height="50" Margin="0,0,20,0" Content="Import Beitie File" FontSize="20" Click="OnImportBeitieFile"/>
                <Button x:Name="ImportBtDir" Width="230" Height="50" Content="Import Beitie Directory" FontSize="20" Click="OnImportBeitieDir"/>
            </StackPanel>
            
            <StackPanel VerticalAlignment="Center" Orientation="Horizontal" HorizontalAlignment="Stretch"  >
                <TextBox x:Name="FileDirPath" Visibility="Collapsed"  VerticalContentAlignment="Center" FontSize="18" VerticalAlignment="Stretch" />
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock  Text="行数：" Margin="0,0,5,0" Style="{StaticResource LeftFriendly}" />
            <ComboBox  x:Name="RowCount" SelectionChanged="RowCount_SelectionChanged"  IsDropDownOpen="True" IsEditable="True" Width="100"/>
            <TextBlock  Text="列数：" Margin="20,0,5,0" Style="{StaticResource LeftFriendly}" />
            <ComboBox  x:Name="ColumnCount" SelectionChanged="CoumnCount_SelectionChanged"  IsDropDownOpen="True" IsEditable="True" Width="100" />
        </StackPanel>
        
        <Border x:Name="PageBorder" Grid.Row="2" Grid.Column="0" BorderBrush="Gray" BorderThickness="1" Margin="3">
            <ScrollViewer x:Name="ImageScrollViewer"  HorizontalScrollBarVisibility="Visible"  VerticalScrollBarVisibility="Visible"
                    ZoomMode="Enabled">

                <canvas:CanvasControl Draw="CurrentPage_OnDraw" 
                                      x:Name="CurrentPage" ClearColor="Gray" />

                <!--<Canvas Name="CurrentPage" RenderTransformOrigin="0.5,0.5"/>-->
                <!--<Image x:Name="CurrentPage" HorizontalAlignment="Center"  VerticalAlignment="Center" Source="/Assets/SplashScreen.png"/>-->
            </ScrollViewer>
            
        </Border>

        <TextBox x:Name="PageText" Grid.Row="2" Grid.Column="1" AcceptsReturn="True" Text="释文" VerticalAlignment="Stretch" Margin="3"/>
    </Grid>
</Page>
